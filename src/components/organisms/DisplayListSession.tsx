import {FlatList} from "react-native";
import React from "react";

export const DisplayListSession = () => {
    return (
        <FlatList
            data={}
            renderItem={}
        />
    );
};
